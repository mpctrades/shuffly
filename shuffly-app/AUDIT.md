# Shuffly — App Store submission audit

Audit only. No code changed. All line numbers are current as of this commit.

## Phase 0 — Orient

| | |
|---|---|
| **Framework** | React Router v7 in "framework mode" (`@react-router/dev`, `@react-router/fs-routes` flat-file routing) — the current Remix successor, not classic Remix. |
| **Shopify library** | `@shopify/shopify-app-react-router` `^1.2.1` (server auth/billing/webhooks) + `@shopify/app-bridge-react` `^4.2.4` (`useAppBridge()` hook only — App Bridge itself loads from the CDN, see A1). |
| **Pinned API version** | Admin GraphQL: `ApiVersion.July26` (`app/shopify.server.ts:78`). Webhooks: `api_version = "2026-07"` (`shopify.app.toml:13`). |
| **Hosting** | Self-hosted single container — `Dockerfile` (node:20-alpine, `npm run build` then `npm run docker-start`). `application_url` is `https://dev.shuffly.mpctrades.com`, which is currently the local `shopify app dev` tunnel, not a separately deployed environment (verified live against this repo's working tree in a prior session). |
| **Database** | SQLite via Prisma (`prisma/schema.prisma:12-13`, `provider = "sqlite"`, `url = "file:dev.sqlite"` — hardcoded, not env-driven; single-file DB, consistent with the single-container Dockerfile). |
| **Background jobs** | In-process `setInterval` poller, one process, no queue (`app/lib/scheduler.server.ts:20-29`, polls every 60s). Fallback for multi-replica/serverless: external cron hits `POST /api/cron/run-shuffles` (`app/routes/api.cron.run-shuffles.tsx`), gated by a `CRON_SECRET` bearer check that fails closed in production if unset (lines 13-26). |
| **shopify.app.toml** | `shuffly-app/shopify.app.toml` (repo root of the app package). |

**Routes** (`app/routes/`, flat-file convention — `app.foo.tsx` = `/app/foo`, nested under the `app.tsx` layout):
- `_index/route.tsx` — public marketing/landing page
- `app.tsx` — embedded layout (nav, Polaris `AppProvider`, auth gate) wrapping every `/app/*` page
- `app._index.tsx`, `app.activity.tsx`, `app.collections.tsx`, `app.collections.$id.tsx`, `app.collections.picker.tsx`, `app.help.tsx`, `app.insights.tsx`, `app.onboarding.tsx`, `app.plan.tsx`, `app.settings.tsx`
- `auth.$.tsx`, `auth.login/route.tsx` — OAuth entry points
- `privacy.tsx` — public privacy policy page
- `api.cron.run-shuffles.tsx` — external cron endpoint (bearer-secret auth, not Shopify session)

**Webhook handlers** (`app/routes/`):
- `webhooks.app.uninstalled.tsx` — deletes the shop's `Session` rows
- `webhooks.app.scopes_update.tsx` — updates the stored session's `scope` string
- `webhooks.compliance.tsx` — `customers/data_request`, `customers/redact`, `shop/redact` (one handler, `compliance_topics`)
- `webhooks.products.update.tsx` — sold-out reaction, fallback trigger
- `webhooks.inventory-levels.update.tsx` — sold-out reaction, primary trigger

---

## Phase 1 — Audit table

### A. Submission blockers

| # | Check | Verdict | Evidence | Fix | Effort |
|---|---|---|---|---|---|
| A1 | App Bridge latest, via CDN `<script>` in `<head>`, on every page | **PASS** | `app/root.tsx:22-29` (`AppBridgeHead` renders `<meta name="shopify-api-key">` + `<script src="https://cdn.shopify.com/shopifycloud/app-bridge.js">`), used at `app/root.tsx:39` (normal render) and `:73` (root `ErrorBoundary`). `root.tsx` is the single document shell for the whole route tree, so every route — including `_index`, `auth.login`, `privacy` — inherits it. `app/routes/app.tsx:78` uses `AppProvider embedded={false}` deliberately, since App Bridge is already active from the CDN script; `@shopify/app-bridge-react`'s `useAppBridge()` (e.g. `app/routes/app.plan.tsx:4,273`) just reads the CDN-loaded instance, it doesn't bundle a second copy. Confirmed live: the deployed `dev.shuffly.mpctrades.com` head matches this exactly. | — | — |
| A2 | Session-token auth (no third-party cookies); OAuth on install **and** reinstall | **PASS** | `app/shopify.server.ts:75-91` configures `shopifyApp(...)` with `PrismaSessionStorage` and `distribution: AppDistribution.AppStore`; every `/app/*` loader calls `authenticate.admin(request)` (e.g. `app/routes/app.tsx:10`, `app.collections.tsx:54,364`, `app.settings.tsx:51,96`, `app.insights.tsx:22,47`, `app.onboarding.tsx:12,31`, `app.activity.tsx:51,81`, `app.help.tsx:143`, `app.collections.$id.tsx:17,67`, `app.collections.picker.tsx:16`) — the library's `authenticate.admin` does the session-token exchange, no cookie/localStorage auth exists anywhere (re-verified: no `document.cookie` / `localStorage` hits in `app/`). Reinstall: no "install-once" flag or first-run gate exists anywhere in the codebase that could block re-authentication; `PrismaSessionStorage` upserts by session id, so a reinstall's fresh OAuth simply overwrites the old row. `future.expiringOfflineAccessTokens: true` is set (`shopify.server.ts:86`), the current recommended default. | — | — |
| A3 | Compliance webhooks exist, registered, verify HMAC, 401 on bad signature | **PASS** | Registered together in `shopify.app.toml:24-27` via `compliance_topics = ["customers/data_request","customers/redact","shop/redact"]`. Handled in `app/routes/webhooks.compliance.tsx:15-47`, switching on `topic` (lines 20-31). HMAC verification and the 401 aren't app code — they're enforced by the library's `authenticate.webhook()` call (`webhooks.compliance.tsx:16`) itself: `node_modules/@shopify/shopify-app-react-router/dist/cjs/server/authenticate/webhooks/authenticate.js:26-29` throws a `Response` with `status: 401` on `WebhookValidationErrorReason.InvalidHmac`, before the handler's own code ever runs. | — | — |
| A4 | All charges via Billing API; merchant can upgrade/downgrade/cancel with no support contact | **PASS** | `app/shopify.server.ts:18-73` defines all plans as `BILLING_PLANS` passed into `shopifyApp({ billing: BILLING_PLANS })` — no non-Shopify billing anywhere. Upgrade: `app/routes/app.plan.tsx:207-224` (`_action=subscribe` → `billing.request(...)`, redirects to Shopify's own approval page). Downgrade/cancel: `app.plan.tsx:226-255` (`_action=downgrade` → `billing.cancel(...)` + local plan update); the "Cancel subscription" link in `BillingInfoCard` (`app.plan.tsx:961-967`) drives the same downgrade-to-FREE path (`onCancel` → `requestSwitch("FREE")`, `app.plan.tsx:535`). All three flows are in-app UI actions, nothing requires contacting support. Declined charges: `billing.request` throws internally on decline (Shopify redirects back), and the `subscribeFetcher` error toast (`app.plan.tsx:324-331`) surfaces any failure without leaving the merchant stuck. | — | — |
| A5 | Scopes are **exactly** `read_products, write_products` | **FAIL** | `shopify.app.toml:10`: `scopes = "read_products,write_products,read_inventory"` — `read_inventory` was added in the previous session to support the `inventory_levels/update` sold-out-reaction webhook (`app/routes/webhooks.inventory-levels.update.tsx:39-58` queries `inventoryItem(id: $id) { variants { nodes { product { totalInventory } } } }`, which needs it). Repo-wide scope search found no other scope string (no `read_orders`, `read_customers`, `read_themes`, etc. anywhere in `.ts`/`.tsx`/`.toml`/`.md`). Runtime scopes come from `process.env.SCOPES` (`app/shopify.server.ts:79`), which isn't set in any repo file (no `.env`, not in `Dockerfile`/`docker-compose.yml`) — `shopify app dev` injects it from the TOML at dev time; in a real deploy, whoever sets `SCOPES` must match the TOML or the two silently drift (**UNKNOWN** what's actually set in your production environment — not verifiable from the repo). | Per your guardrails I'm not touching scopes myself. Two real options: (a) drop `read_inventory` and the whole `inventory_levels/update` webhook, reverting to `products/update`-only sold-out detection (slower, ~1 min instead of near-instant, but keeps the two-scope claim exactly true), or (b) keep the scope and update this requirement's expectation — `read_inventory` is a normal, justifiable scope for this feature and reviewers do accept scopes with a demonstrated need. Your call — either is a real code change, not a one-liner. | (a) ~30 min: delete the webhook route + toml entry + revert `webhooks.products.update.tsx` to its pre-inventory-webhook form. (b) 0 min code, just re-scope the requirement. |
| A6 | No Admin API call on a >90-day deprecating endpoint; specifically `collectionAddProductsV2`→`collectionUpdate`, `ruleSet`→sources/conditions | **PASS** | Repo-wide search for `collectionAddProductsV2` and `ruleSet`/`CollectionRuleSet`: zero hits. Collection writes go through `collectionUpdate` (`app/lib/collections.server.ts:424`) and `collectionReorderProducts` (`app/lib/collections.server.ts:469`) — neither is on Shopify's deprecation list. Pinned version `2026-07`/`July26` is a current, non-deprecated release line. | — | — |
| A7 | Clean uninstall: `app/uninstalled` exists; `shop/redact` hard-deletes every shop row (snapshots, schedules, position history, settings, jobs) with nothing orphaned | **PASS, with one caveat noted below** | `app/routes/webhooks.app.uninstalled.tsx:13-15` deletes `Session` rows on uninstall (correct — Shopify allows up to 48h before requiring full erasure, and Shuffly's own Settings copy promises exactly that). Full erasure happens on `SHOP_REDACT` in `webhooks.compliance.tsx:28-30`: `db.collectionConfig.deleteMany`, `db.shopSettings.deleteMany`, `db.session.deleteMany`. It does **not** explicitly delete `ShuffleRun`, `ProductExposure`, `PositionSnapshot`, or `ProductPosition` — but all four declare `onDelete: Cascade` on their FK to `CollectionConfig` (`prisma/schema.prisma:105,132,153,176`), and the actual SQL constraints exist at the DB level (`ON DELETE CASCADE`, confirmed in `prisma/migrations/20260821085320_shuffly_core/migration.sql:57`, `.../20260824061639_insights_position_history/migration.sql:12,23`, `.../20260827020910_product_position_and_page_size/migration.sql:11`), so deleting `CollectionConfig` should cascade-delete all four automatically. **Caveat:** this relies on SQLite's `PRAGMA foreign_keys = ON` being active on Prisma's runtime connection (Prisma's default `relationMode = "foreignKeys"` for the `sqlite` provider — not overridden anywhere in `schema.prisma`) — I could not find a test or runtime check in this repo that actually proves the cascade fires outside of a migration script. No other persisted state exists outside these Prisma models (no separate job/queue table, no file storage). | Cheap insurance, doesn't change behavior if the cascade already works: add one integration test that creates a `CollectionConfig` with child rows in all four tables, deletes it, and asserts the children are gone — turns this from "should cascade" into "proven to cascade." | ~20 min |
| A8 | No Asset API, ScriptTag, theme app extensions, or storefront JS | **PASS** | `extensions/` contains only a `.gitkeep` (no `shopify.extension.toml` anywhere in the repo). Repo-wide search for `ScriptTag`, `themeCreate`, `asset` + `put`/`create` theme calls: zero hits. Scopes (`read_products,write_products,read_inventory`) have no theme/asset scope. Nothing in `app/` renders or ships JS to the storefront — every route under `app/routes/` is an Admin-embedded or server-only (webhook/cron) route. | — | — |

### B. Built for Shopify (not required to submit)

| # | Check | Verdict | Evidence | Fix | Effort |
|---|---|---|---|---|---|
| B9 | Long lists paginated/virtualized, never fully rendered | **FAIL (one list), PASS (the other)** | The collection-picker's search of *all* shop collections is capped and reports overflow: `app/lib/collections.server.ts:67-115` (`listAllCollections`, default `limit = 100`, returns `hasMore`). A single collection's product preview is capped too: `app/routes/app.collections.$id.tsx:47` (`take: 20`), rendering only `preview.length` of `productCount` (line 344). But the merchant's own **tracked-collections list** — the main Collections page — has no cap at all: `app/routes/app.collections.tsx:59`, `db.collectionConfig.findMany({ where: { shop }, orderBy: { createdAt: "asc" } })`, no `take`/pagination, rendered as a plain `.map()` with no windowing library in `package.json`. Free/Starter are capped at 25/100 collections so this is currently harmless, but Pro/Agency are "Unlimited collections" (`app/routes/app.plan.tsx:57`) — a large Pro shop could render 1,000+ rows in one page load. | Add `take`/cursor pagination (or a "load more") to the `app.collections.tsx` loader query and its list UI, same pattern already used in `listAllCollections`. | ~1-2 hrs (loader pagination + UI "load more"/pager + a test) |
| B10 | Images/async content reserve space (no CLS) | **PASS (spot-checked)** | All `<img>` tags found set explicit `width`/`height`: `app/components/CollectionRow.tsx:213-220` (`THUMB_SIZE` both dims), `app/routes/app.insights.tsx:579` (`width={32} height={32}`), `app/routes/_index/route.tsx:27` (`width={64} height={64}`). Skeleton/loading placeholders exist for the heavier async pages: `app.plan.tsx` (`PlanSkeleton`), `app.activity.tsx`, `app.collections.tsx`, `app.settings.tsx` (all contain a `Skeleton`-named component, grep-confirmed). Not exhaustively checked: `app.insights.tsx` and `app.onboarding.tsx` have no `Skeleton` hit — worth a manual look, but no image/async pattern found there that looks CLS-risky either. | — | — |
| B11 | Forms use a Polaris contextual save bar | **PASS (main forms), gap elsewhere** | Confirmed `ui-save-bar` in `app/routes/app.collections.$id.tsx` and `app/routes/app.settings.tsx` (the two forms with real "unsaved changes" state). `app.onboarding.tsx`'s wizard and `app.plan.tsx`'s plan-switch buttons act immediately on click rather than holding pending form state, so a save bar doesn't obviously apply there — not flagged as a gap, just not applicable the same way. | — | — |
| B12 | App Bridge nav menu items registered on load, before the merchant visits each page | **PASS** | `<s-app-nav>` with all six `<s-link>` entries is rendered directly in `app/routes/app.tsx:35-43`, inside the layout that wraps *every* `/app/*` route — it mounts on first load of any app page, not per-subpage. | — | — |
| B13 | Every page usable at mobile widths | **PARTIAL / UNKNOWN** | Viewport meta present (`app/root.tsx:38`, `app/root.tsx:72`). `@media` breakpoints found in `app.activity.tsx`, `app.help.tsx`, `app.plan.tsx`, `app.settings.tsx`, `_index/styles.module.css` (e.g. `app.plan.tsx:549`, `.shuffly-plan-row { grid-template-columns: 1fr !important; }` under `max-width: 700px`). No `@media` hits in `app.collections.tsx`, `app.collections.$id.tsx`, `app.insights.tsx`, `app.onboarding.tsx`, `app.collections.picker.tsx` — these may rely on Polaris's own responsive components rather than custom CSS, but I can't confirm actual mobile rendering without opening them at a mobile viewport. | Load each of the 5 un-checked pages at a 375px-wide viewport and look for overflow/clipping. | ~30 min manual pass, no code guaranteed needed |

---

## Summary

| | Count |
|---|---|
| **A — PASS** | 7 of 8 (A1, A2, A3, A4, A6, A7, A8) |
| **A — FAIL** | 1 of 8 (A5 — `read_inventory` scope beyond the stated two) |
| **A — UNKNOWN** | 1 sub-item (production `SCOPES` env value — not visible from the repo) |
| **B — PASS** | 3 of 5 (B10, B11, B12) |
| **B — FAIL** | 1 of 5 (B9 — unpaginated tracked-collections list) |
| **B — PARTIAL/UNKNOWN** | 1 of 5 (B13 — mobile not verified on 5 pages) |

**The one real submission blocker is A5.** Everything else in Section A is solid. A5 is a direct consequence of last session's `inventory_levels/update` feature work and needs your decision (drop the scope, or accept it and reframe the "exactly two scopes" requirement) before anything else — I haven't changed any code.
