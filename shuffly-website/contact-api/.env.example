# Gmail SMTP credentials for the mailbox that sends and receives enquiries.
# SMTP_PASS is a Google App Password, not the account password.
SMTP_USER=team@mpctrades.com
SMTP_PASS=
SMTP_FROM=team@mpctrades.com
SMTP_TO=team@mpctrades.com
